package exam02;

public class Ex02 {
    public static void main(String[] args) {
        Student s1 = new Student(); // 생성자 메서드(함수)
        s1.showInfo();
        // 값을 Student 클래스에서 초기화했기때문에 따로 값을 넣지 않아도 기본값이 있다
    }
}
